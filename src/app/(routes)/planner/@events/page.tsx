import { Card } from "@tremor/react";

export default async function Events() {
  // const hello = await api.post.hello.query({ text: "from tRPC" });
  //  const cashflowEvents = await api.cashflow.get_events.useQuery();

  return (
    <Card>
      <div>Events</div>
    </Card>
  );
}
